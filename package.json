{
  "name": "papeors",
  "version": "1.0.0",
  "description": "A web-based OS",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "rm -r ./frontend/builds && mkdir ./frontend/builds && cd ./frontend/src && zip ../builds/paperOS.zip -r ./paperOS"
  },
  "author": "mcbutnugget",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "eslint": "^9.38.0",
    "express": "^5.1.0"
  }
}
